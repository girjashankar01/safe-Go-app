#Buld Process

#Phase 0 (setup):

created backend, dashboard, mobile directory and ran following commands from safe go app directory:
1. // this will use default one: (not recommended) npx create-expo-app mobile
1. npx create-expo-app safego-mobile --template blank //use this instead
2. npm create vite@latest safego-dashboard -- --template react


# Building Mobile app
